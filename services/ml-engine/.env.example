# Local dev (outside Docker) — Postgres runs on localhost via make db-up
DATABASE_URL=postgresql://prism:prism_dev_secret@localhost:5432/prism

NOAA_API_KEY=    # get free token at https://www.ncei.noaa.gov/cdo-web/token
# FEMA and USGS require no API key
